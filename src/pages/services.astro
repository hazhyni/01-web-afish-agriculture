---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Button from '../components/Button.astro';
import Footer from '../components/Footer.astro';

const images = [
  '/img/farm.jpg',
  '/img/padi.jpg',
  '/img/rice.jpg', 
  '/img/tea.jpg',
  '/img/teh.jpg',
  '/img/wheat.jpg',
  '/img/check.jpg'
];
---

<Layout title="Services - AGRISPACE">
  <style>
    :root {
      --brand-grad: linear-gradient(135deg, #16a34a 0%, #22c55e 100%);
      --yellow: #22c55e;
    }
    
    body {
      background: linear-gradient(135deg, #0f1a02 0%, #1a2e05 100%);
      color: #f7fee7;
    }
    


    /* HERO */
    .hero {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 1.2fr 1fr;
    }
    .hero-left {
      background: linear-gradient(135deg, #0f1a02 0%, #1a2e05 100%);
      display: grid;
      align-content: center;
      padding: clamp(24px, 6vw, 120px);
    }
    .eyebrow {
      color: var(--yellow);
      text-transform: uppercase;
      letter-spacing: 0.15em;
      font-weight: 700;
      font-size: 12px;
    }
    .title {
      font-size: clamp(40px, 7vw, 96px);
      line-height: 0.98;
      font-weight: 800;
      letter-spacing: -0.02em;
      color: #f7fee7;
    }
    .lead {
      color: #d9f99d;
      font-size: clamp(16px, 1.6vw, 22px);
      max-width: 56ch;
    }
    .actions {
      display: flex;
      gap: 12px;
      margin-top: 20px;
    }
    
    .actions button, .actions a {
      transition: all 0.3s ease;
    }
    
    .actions button:hover, .actions a:hover {
      transform: translateY(-2px);
    }

    .bullets {
      margin-top: 20px;
      color: #a3a3a3;
      font-size: 14px;
    }
    .bullets b {
      color: var(--yellow);
    }

    /* RIGHT: animated collage */
    .hero-right {
      position: relative;
      overflow: hidden;
      background: radial-gradient(
          800px 400px at 100% 0%,
          rgba(34, 197, 94, 0.2),
          transparent
        ),
        radial-gradient(
          700px 380px at 0% 100%,
          rgba(22, 163, 74, 0.1),
          transparent
        ),
        #1a2e05;
    }
    .stack {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
    }
    .frame {
      position: absolute;
      width: min(480px, 42vw);
      aspect-ratio: 16/10;
      border-radius: 18px;
      overflow: hidden;
      border: 1px solid rgba(34, 197, 94, 0.3);
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4);
      transform-origin: center;
      opacity: 0.95;
      background: #2d4a0a;
      animation: floatY 9s ease-in-out infinite var(--delay, 0ms);
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .frame:hover {
      transform: scale(1.05);
      box-shadow: 0 40px 80px rgba(34, 197, 94, 0.2);
      border-color: rgba(34, 197, 94, 0.6);
    }
    .frame img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      filter: saturate(1.05) contrast(1.05);
    }

    /* Parallax layers */
    .f1 {
      transform: translate(-12%, -10%) rotate(-2deg);
      --delay: 0ms;
      z-index: 3;
    }
    .f2 {
      transform: translate(16%, 4%) rotate(2deg);
      --delay: 300ms;
      z-index: 2;
    }
    .f3 {
      transform: translate(0%, 22%) rotate(-1deg);
      --delay: 600ms;
      z-index: 1;
    }

    /* Slow slide background strip */
    .strip {
      position: absolute;
      inset: -10% -20%;
      display: flex;
      gap: 18px;
      transform: rotate(-12deg);
      opacity: 0.9;
    }
    .strip .thumb {
      flex: 0 0 360px;
      height: 220px;
      border-radius: 14px;
      overflow: hidden;
      border: 1px solid rgba(34, 197, 94, 0.2);
    }
    .strip .thumb img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .strip.a {
      top: 10%;
      animation: slideX 40s linear infinite;
    }
    .strip.b {
      bottom: 8%;
      animation: slideX 46s linear infinite reverse;
    }

    @keyframes slideX {
      from {
        transform: translateX(0) rotate(-12deg);
      }
      to {
        transform: translateX(-50%) rotate(-12deg);
      }
    }
    @keyframes floatY {
      0%,
      100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-8px);
      }
    }

    @media (max-width: 960px) {
      .hero {
        grid-template-columns: 1fr;
      }
      .hero-right {
        min-height: 70vh;
      }
      .hero-left {
        padding: 56px 24px;
      }
    }

    /* Respect reduced motion */
    @media (prefers-reduced-motion: reduce) {
      .frame,
      .strip {
        animation: none !important;
      }
    }
  </style>

  <Navbar />

  <!-- HERO ala Squarespace: teks besar kiri, kolase bergerak kanan -->
  <section class="hero">
    <div class="hero-left">
      <div class="eyebrow">Template agrikultur — tanpa ribet</div>
      <h1 class="title">
        Buat website<br />agrikultur <span
          style="background:var(--brand-grad); -webkit-background-clip:text; background-clip:text; color:transparent;"
          >modern</span
        >
      </h1>
      <p class="lead mt-3">
        Template yang mudah diedit, tanpa coding. Fitur komersial & pemasaran
        lengkap untuk usaha pangan dan hortikultura.
      </p>
      <div class="actions">
        <Button variant="primary" href="#templates">Lihat Template</Button>
        <Button variant="ghost" href="#demo">Coba Demo</Button>
      </div>
      <div class="bullets mt-3">
        Free trial, tanpa kartu kredit • Optimasi SEO • Integrasi WhatsApp &
        Marketplace
      </div>
    </div>

    <div class="hero-right">
      <!-- slow background strips of thumbs -->
      <div class="strip a">
        {
          images.slice(0, 6).map((src) => (
            <div class="thumb">
              <img src={src} alt="Agriculture" loading="lazy" />
            </div>
          ))
        }
      </div>
      <div class="strip b">
        {
          images.slice(3, 9).map((src) => (
            <div class="thumb">
              <img src={src} alt="Agriculture" loading="lazy" />
            </div>
          ))
        }
      </div>

      <!-- floating main frames (like Squarespace previews) -->
      <div class="stack">
        <figure class="frame f1">
          <img src={images[0]} alt="Preview 1" />
        </figure>
        <figure class="frame f2">
          <img src={images[4]} alt="Preview 2" />
        </figure>
        <figure class="frame f3">
          <img src={images[7]} alt="Preview 3" />
        </figure>
      </div>
    </div>
  </section>

  <!-- Interactive Features Section -->
  <section
    id="fitur"
    style="padding:80px 24px; background:#1a2e05; border-top:1px solid rgba(34,197,94,.2)"
  >
    <div style="max-width:1100px; margin:0 auto">
      <h2 style="font-size:36px; font-weight:800; letter-spacing:-.02em; color:#f7fee7; text-align:center; margin-bottom:48px">
        Fitur untuk rantai pasok
      </h2>
      <div style="display:grid; grid-template-columns:repeat(auto-fit, minmax(300px, 1fr)); gap:32px">
        <div class="feature-card" style="padding:32px; background:rgba(34,197,94,0.1); border-radius:16px; border:1px solid rgba(34,197,94,0.2); cursor:pointer; transition:all 0.3s ease">
          <div style="font-size:48px; margin-bottom:16px">📊</div>
          <h3 style="color:#22c55e; font-weight:700; margin-bottom:12px">Dasbor Real-time</h3>
          <p style="color:#d9f99d; margin:0; line-height:1.6">Monitor harga & stok secara real-time dengan dashboard interaktif</p>
        </div>
        <div class="feature-card" style="padding:32px; background:rgba(34,197,94,0.1); border-radius:16px; border:1px solid rgba(34,197,94,0.2); cursor:pointer; transition:all 0.3s ease">
          <div style="font-size:48px; margin-bottom:16px">🚚</div>
          <h3 style="color:#22c55e; font-weight:700; margin-bottom:12px">Pelacakan Pengiriman</h3>
          <p style="color:#d9f99d; margin:0; line-height:1.6">Tracking pengiriman & monitoring suhu untuk menjaga kualitas</p>
        </div>
        <div class="feature-card" style="padding:32px; background:rgba(34,197,94,0.1); border-radius:16px; border:1px solid rgba(34,197,94,0.2); cursor:pointer; transition:all 0.3s ease">
          <div style="font-size:48px; margin-bottom:16px">🌾</div>
          <h3 style="color:#22c55e; font-weight:700; margin-bottom:12px">Katalog Produk</h3>
          <p style="color:#d9f99d; margin:0; line-height:1.6">Katalog lengkap hasil panen dengan detail kualitas dan asal</p>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Call to Action Section -->
  <section style="padding:80px 24px; background:#0f1a02; text-align:center">
    <div style="max-width:800px; margin:0 auto">
      <h2 style="color:#f7fee7; font-size:32px; font-weight:800; margin-bottom:20px">
        Siap Memulai?
      </h2>
      <p style="color:#d9f99d; font-size:18px; margin-bottom:40px">
        Bergabunglah dengan ratusan petani dan distributor yang telah mempercayai layanan kami
      </p>
      <div style="display:flex; gap:16px; justify-content:center; flex-wrap:wrap">
        <Button variant="primary" href="#kontak">Mulai Sekarang</Button>
        <Button variant="ghost" href="#demo">Lihat Demo</Button>
      </div>
    </div>
  </section>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      // Image rotation for frames
      const list = Array.from(document.querySelectorAll(".frame img"));
      const pool = [
        "/img/farm.jpg",
        "/img/padi.jpg", 
        "/img/rice.jpg",
        "/img/tea.jpg",
        "/img/teh.jpg",
        "/img/wheat.jpg",
        "/img/check.jpg"
      ];
      let ptr = 0;
      setInterval(() => {
        const target = list[Math.floor(Math.random() * list.length)] as HTMLImageElement;
        target.style.transition = "opacity .6s ease";
        target.style.opacity = "0";
        setTimeout(() => {
          target.src = pool[ptr % pool.length];
          ptr++;
          target.style.opacity = "1";
        }, 320);
      }, 3800);
      
      // Feature cards hover effects
      const featureCards = document.querySelectorAll('.feature-card');
      featureCards.forEach(card => {
        card.addEventListener('mouseenter', () => {
          (card as HTMLElement).style.transform = 'translateY(-8px)';
          (card as HTMLElement).style.boxShadow = '0 20px 40px rgba(34, 197, 94, 0.2)';
          (card as HTMLElement).style.borderColor = 'rgba(34, 197, 94, 0.4)';
        });
        
        card.addEventListener('mouseleave', () => {
          (card as HTMLElement).style.transform = 'translateY(0)';
          (card as HTMLElement).style.boxShadow = 'none';
          (card as HTMLElement).style.borderColor = 'rgba(34, 197, 94, 0.2)';
        });
      });
    });
  </script>
  
  <Footer />
</Layout>